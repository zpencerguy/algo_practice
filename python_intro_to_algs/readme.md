Steps to setup environment to run scripts:

Environment creation
> conda env create -f config/environment.yml

To activate this environment, use:
> source activate oop_demo

To deactivate an active environment, use:
> source deactivate